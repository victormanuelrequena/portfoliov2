import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

const intialWorksInfoState = [
  {
    
  }
]

export const useCounterStore = defineStore('works', () => {
  const worksInfo = []

  return {}
})
